# Step 6 Streamlit strategy monitor dependencies, pinned.
# Not to be confused with dashboard/requirements.txt, which belongs to the
# Dash research dashboard. See requirements.txt for the pinning rationale.
streamlit==1.62.0
requests==2.34.2
pandas==2.3.3
plotly==6.9.0
